package nl.pocketquest.pocketquest.views.main.recipe

import nl.pocketquest.pocketquest.views.BaseFragment

class RecipeFragment : BaseFragment(), RecipeContract.RecipeView {

    override fun addRecipeModel(recipeModel: RecipeContract.RecipeModel) {
    }

    override fun updateRecipeModel(recipeModel: RecipeContract.RecipeModel, newRecipeModel: RecipeContract.RecipeModel) {
    }

    override fun removeRecipeModel(recipeModel: RecipeContract.RecipeModel) {
    }

    override fun setFilter(filterDescription: String) {
    }
}
